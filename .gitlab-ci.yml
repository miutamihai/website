build:
  stage: build
  image: elixir:latest
  script: 
    - mix local.rebar --force
    - mix local.hex --force
    - mix deps.get
    - mix site.build
  artifacts:
    paths:
      - output

deploy:
  stage: deploy
  image: node:lts
  script: echo "Disabled for now"
