build:
  stage: build
  image: elixir:latest
  script: 
    - mix local.rebar --force
    - mix local.hex --force
    - mix deps.get
    - mix site.build

deploy:
  stage: deploy
  image: node:lts
  script: npx wrangler pages deploy output --project-name ivaldea-website
