image: elixir:latest

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

build:
  stage: build
  script: mix site.build

deploy:
  stage: deploy
  image: node:lts
  script: npx wrangler deploy
